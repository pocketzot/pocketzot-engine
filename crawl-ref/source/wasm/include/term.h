/* Empty terminfo stub for the PocketZot WASM build: libunix.cc includes
 * <term.h> but uses none of its declarations, and the Emscripten sysroot
 * has no terminfo. Served from wasm/include via -isystem. */
